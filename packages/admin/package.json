{
  "name": "@erp/admin",
  "version": "1.0.0",
  "private": true,
  "description": "ERP Admin Console — Tenant management, monitoring, system config",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": { ".": "./src/index.ts" },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@erp/shared": "*",
    "@erp/database": "*",
    "@erp/events": "*",
    "@erp/feature-flags": "*"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.6.0"
  }
}
